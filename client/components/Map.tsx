import React from "react";
import { MapContainer, TileLayer, Marker } from "react-leaflet";

const Map = () => {
  return (
    <section>
      <h2>Maps</h2>
    </section>
  );
};

export default Map;
